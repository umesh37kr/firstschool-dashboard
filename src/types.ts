export interface Students {
  _id: string;
  avatar: string;
  rollNumber: number;
  firstName: string;
  lastName: string;
  gender: string;
  classes: string;
  section: string;
  dateOfBirth: string;
  mobile: number;
  address: string;
  createdAt: string;
}
