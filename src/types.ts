export interface Students {
  _id: string;
  avatar: string;
  rollNumber: number;
  firstName: string;
  lastName: string;
  gender: string;
  classes: string;
  section: string;
  dateOfBirth: string;
  address: string;
  createdAt: string;
}
